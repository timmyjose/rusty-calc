// import { expect } from 'detox'
import { test, describe } from '@jest/globals'
// import { iteratee } from 'lodash'

describe('Home screen', () => {
  //   beforeAll(async () => {
  //     await device.launchApp({
  //       newInstance: true,
  //       launchArgs: { detoxURLBlacklistRegex: '\\("^https://firestore\.googleapis\.com/.*"\\)' },
  //       permissions: { notifications: 'YES', userTracking: 'YES' }
  //     })
  //   })

  //   beforeEach(async () => {
  //     await device.reloadReactNative()
  //   })

  test('should test something', async () => { })

  //   /* Visibility tests */

  //   // it('"QR Scan" text must be visible', async () => {
  //   //    // skip the intro screens
  //   //    await expect(element(by.text('QR Scan'))).toBeVisible()
  //   // })

  //   // it('"Wallet" text must be visible', async () => {
  //   //    await expect(element(by.text('Wallet'))).toBeVisible()
  //   // })

  //   // it('"Activity" text must be visible', async () => {
  //   //    await expect(element(by.text('Activity'))).toBeVisible()
  //   // })
})

